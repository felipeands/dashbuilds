export enum BoardStatus {

  // firewall
  "accepted"  = 1,
  "rejected"  = 2,

  // build
  "succeed"   = 3,
  "fail"      = 4,

  // proccessing
  "running"   = 5,

  // waiting
  "waiting"   = 6
}
